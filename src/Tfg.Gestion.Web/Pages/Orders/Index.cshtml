@page
@using Tfg.Gestion.Localization
@using Tfg.Gestion.Web.Pages.Orders
@using Microsoft.Extensions.Localization
@model IndexModel
@inject IStringLocalizer<GestionResource> L

@section styles
{
    <link rel="stylesheet" href="/Pages/Orders/Styles.css" />
}

@section scripts
{
    <abp-script src="/Pages/Orders/Index.js" />
}

<abp-card>
    <abp-card-header>
        <abp-row>
            <abp-column size-md="_6">
                <abp-card-title>@L["Orders"]</abp-card-title>
            </abp-column>
            <abp-column size-md="_6" class="text-end">
                <abp-button id="NewOrderButton"
                            text="@L["NewOrder"].Value"
                            icon="plus"
                            button-type="Primary" />
            </abp-column>
        </abp-row>
        <abp-row>
            <abp-column size-md="_6">
                <input id="searchInput" type="text" class="form-control" placeholder="@L["Search"]">
            </abp-column>
            <abp-column size-md="_6">
                <select id="categorySelect" class="form-control">
                    <option value="">@L["AllCategories"]</option>
                    <!-- Aquí puedes incluir opciones para cada categoría si es aplicable -->
                </select>
            </abp-column>
        </abp-row>
    </abp-card-header>
    <abp-card-body>
        <div class="table-responsive">
            <table class="table table-striped" id="OrdersList">
                <thead>
                    <tr>
                        <th>@L["pedidoId"]</th>
                        <th>@L["customerName"]</th>
                        <th>@L["userName"]</th>
                        <th>@L["orderStatus"]</th>
                        <th>@L["dateDisplay"]</th>
                    </tr>
                </thead>
                <tbody>
                    <!-- Aquí se cargarán dinámicamente las filas de la tabla -->
                </tbody>
            </table>
        </div>
    </abp-card-body>
</abp-card>